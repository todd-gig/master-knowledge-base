---
title: 10-Day Revenue Plan — Outstanding Work → Deployment → Predictable Revenue
created: 2026-05-07 (revised)
owner: todd@gigaton.ai
status: active
horizon: 2026-05-07 → 2026-05-16
supersedes: 2026-05-07 morning version (process-only framing)
companion: STATE_OF_UNION_2026-05-07.md (verified ground truth)
---

# Goal restated

Convert ~18 active repos + ~25 Desktop bundles + 3 ongoing campaigns into one coherent execution: all outstanding work landed, all production services deployed clean, and at least one **predictable, recurring revenue stream** live by 2026-05-16. "Predictable" means: a repeatable cash inflow with billing infrastructure, not a one-off invoice.

Process foundation (CODEOWNERS, deploy script, SERVICES.md) is **necessary but not the goal**. It exists to make the revenue paths shippable on the daily 19:00 CT cadence without breakage.

# Locked decisions (carry-forward, do not re-ask)

1. **GCP owner of record:** `todd@gigaton.ai`. `todd.cx@turtleisland.solutions` IAM Viewer only.
2. **Domain strategy:** per-service custom domains (`api/app/decide/sales/sie/docs.gigaton.ai` + `playadelcarmen.homes` + `admin.playadelcarmen.homes`).
3. **Docs site:** Cloud Run + mkdocs-material container.
4. **Daily deploy:** manual trigger at or by 19:00 CT. Past 20:00 = skip, deploy in morning. No cron.
5. **CODEOWNERS:** `@todd-gig` + `@bella-byte` on every production repo.

# Revenue paths (chosen — see STATE_OF_UNION for full list)

**Primary (Days 1–7):** Carmen Beach property bookings — `playadelcarmen.homes` live, payment processor go-live, first paid booking by 2026-05-13.

**Secondary (Days 3–10):** GigNet usage + billing dashboard implemented in `Gigaton-UI-Platform` — entitlements + Stripe webhooks + dashboard cards. First paying tenant by 2026-05-16.

**Tertiary (Days 4–10):** SIE Fiduciary Risk Audit campaign — landing page live, email sequence dripped, 1+ booked discovery call by 2026-05-16.

**Compound (existing):** Ti Solutions SalesOS bridge — wire 214-item catalog into Ti's existing dialer/HubSpot to compound an already-revenue business.

# Critical Day-0 decisions queued for user

These are the 5 questions in `STATE_OF_UNION_2026-05-07.md`. Auto mode is on; in their absence I will execute on the recommended defaults marked there.

---

# Day 1 (Thu 2026-05-08) — Unblock everything

**Goal:** No more dirty trees, no more stuck rebases, full ground truth on Cloud Run live state.

| # | Task | Owner | Done when |
|---|---|---|---|
| 1.1 | `gcloud auth login` (interactive) | **user** | `gcloud projects list` returns clean |
| 1.2 | SIE rebase: abort + plan as PR | claude | `git status` clean on `master` and `main`, P0 work captured in branch |
| 1.3 | Parent monorepo sweep triage: per-submodule decide land/branch/revert | claude (with user confirm per submodule) | `git status` at `~/Documents/GitHub` is clean |
| 1.4 | Move `client_secret_*.json` off Desktop into `gcloud secrets` or 1Password | claude | File no longer on Desktop; Secret Manager has equivalent |
| 1.5 | Fill `SERVICES.md` from gcloud output (services, URLs, domains, secrets) | claude | All `_fill_` cells replaced |
| 1.6 | Commit Day-0 governance files (`PROCESS.md`, `PRINCIPLES.md`, `CODEOWNERS.template`, `scripts/*`) to master-knowledge-base on `main` via PR | claude | PR open; CODEOWNERS bot mentions `@todd-gig` `@bella-byte` |
| 1.7 | Identify Carmen Beach DNS owner + playadelcarmen.homes state (registered? where?) | user | DNS provider + zone confirmed |

**Revenue gate:** none yet — pure unblock day.

**Exit criteria:** all repos clean. SIE rebase resolved. SERVICES.md complete. playadelcarmen.homes DNS path confirmed. Day-0 PR open.

---

# Day 2 (Fri 2026-05-09) — Carmen Beach goes public

**Goal:** playadelcarmen.homes resolves to the production web app. Bookings can be taken from a real domain.

| # | Task | Done when |
|---|---|---|
| 2.1 | DNS records for `playadelcarmen.homes` + `admin.playadelcarmen.homes` → Cloud Run | `dig playadelcarmen.homes` returns Cloud Run hostname |
| 2.2 | Custom domain mapping in Cloud Run (`gcloud run domain-mappings create`) | TLS cert provisioned, 200 on / |
| 2.3 | Stripe (or chosen processor) test mode → live mode for Carmen Beach LLC | Test charge succeeds; refund tested |
| 2.4 | Seed first 3 properties with real data (pricing, photos via Matterport links, availability) | Properties visible at playadelcarmen.homes |
| 2.5 | Affiliate-tier program: onboard 3 first affiliates (Tier 1, ≤5 outstanding-invite cap) | 3 affiliate dashboards active |
| 2.6 | Apply CODEOWNERS to all 9 production repos via `install-codeowners.sh --apply` | All PRs require `@todd-gig` + `@bella-byte` review |
| 2.7 | Branch protection on `main` for all 9 repos | Direct push to main blocked |

**Revenue gate:** playadelcarmen.homes live with bookings enabled. **First booking-capable URL by 17:00 CT.**

---

# Day 3 (Sat 2026-05-10) — Deploy automation green

**Goal:** `make deploy` works in every backend; `deploy_19.sh` runs full stack <15 min from clean main.

| # | Task | Done when |
|---|---|---|
| 3.1 | Standardize `cloudbuild.yaml` + `Makefile` across `decision-engine`, `gigaton-engine`, `sales-operating-system`, `intelligence-silo` | `cd <repo> && make deploy` works |
| 3.2 | Wire `intelligence-silo` Cloud Run env: DECISION_ENGINE_URL, GCS volume, ANTHROPIC_API_KEY, VAULT_PASSPHRASE | `/health` returns 200 with deps reachable |
| 3.3 | `/health` + `/version` on every backend (returns git SHA + image tag) | All 4 services pass |
| 3.4 | Post-deploy smoke test in cloudbuild (calls `/health` of new revision before traffic promote) | New revision rejected if smoke fails |
| 3.5 | Time slowest service end-to-end. Target 12 min, cap 15 | Logged in `deploy-log.jsonl` |
| 3.6 | `deploy_19.sh` — refuses dirty trees, refuses past 20:00 CT, refuses if previous day not green | First successful 19:00 CT run |

**Revenue gate:** deploy ritual working — protects revenue paths from breakage.

---

# Day 4 (Sun 2026-05-11) — Billing schema + Stripe webhook foundation

**Goal:** Database tables, schemas, and Stripe webhook endpoint live in `Gigaton-UI-Platform` so the billing dashboard can be built on top of real data.

| # | Task | Done when |
|---|---|---|
| 4.1 | Read `gignet_usage_billing_dashboard_claude_bundle/db/` + `schemas/` — implement migrations in Gigaton-UI-Platform's existing migration system (do NOT create parallel architecture per the bundle's own instruction) | Migrations applied in staging |
| 4.2 | Stripe products + prices defined for: Gigaton platform tier, Carmen Beach property booking, SalesOS pilot | Prices visible in Stripe dashboard |
| 4.3 | Stripe webhook endpoint in Gigaton-UI-Platform (handle `invoice.paid`, `customer.subscription.created`, `customer.subscription.deleted`, `charge.failed`) | Webhook receives and stores test events |
| 4.4 | Entitlements model: which Stripe price → which feature flags | Entitlements table populated |
| 4.5 | Frontend `make deploy` for `gigaton`, `gigaton-ui-system` (npm publish), Carmen Beach web/admin | All 4 ship via `deploy_all.sh` |
| 4.6 | `deploy_all.sh` — sequential full-stack with same release ID; auto-rollback on `/version` mismatch | Full stack <15 min wall-clock |

**Revenue gate:** billing infrastructure can RECEIVE money. (Cannot yet bill — that's Day 5.)

---

# Day 5 (Mon 2026-05-12) — Billing dashboard live + decision spine integration

**Goal:** GigNet users can see usage + invoices + entitlements. SIE → decision-engine → silo audit chain visible.

| # | Task | Done when |
|---|---|---|
| 5.1 | Implement billing dashboard cards (per `gignet_usage_billing_dashboard_claude_bundle/ui/`): usage, invoices, entitlements, upgrade actions | Dashboard renders for any signed-in tenant |
| 5.2 | Connect cards to data: usage from observability, invoices from Stripe, entitlements from DB | All 4 cards show real data for at least 1 test tenant |
| 5.3 | First Carmen Beach booking processed end-to-end: web booking → Stripe charge → email confirmation → admin dashboard reflects it | Test booking visible in admin |
| 5.4 | Fold `claude_decision_logic_pack` (already its own repo) as canonical scoring spec for `decision-engine`. Reconcile diff. | Diff zero or documented |
| 5.5 | SIE operator-api → decision-engine `/decide` for every queued item. Capture QC→VC→TC→EC chain in audit log. | `/sie-audit` shows full chain on a synthetic item |
| 5.6 | Integration test: synthetic lead → SIE queue → decision → silo critique → final verdict + cert chain | Test passes; latency p95 <3s |

**Revenue gate:** dashboard is usable; first **real** Carmen Beach booking expected today (not synthetic).

---

# Day 6 (Tue 2026-05-13) — Memory unification + first paid customer day

**Goal:** Memory layer unified. First paying customer (any path).

| # | Task | Done when |
|---|---|---|
| 6.1 | Fold `TI_Memory_Network_Claude_Bundle_v3` into `master-knowledge-base/memory/` as canonical contract | Bundle deleted from Desktop; `memory/` has the contract |
| 6.2 | 4-layer memory hierarchy: working/episodic/semantic/procedural. Map `transcript-knowledge-base`, `braintrust-knowledge-base`, TI Memory entities into it. | Each source mapped in `memory/MAPPING.md` |
| 6.3 | `/memory/recall` endpoint on intelligence-silo with perspective filtering. decision-engine consumes via `MEMORY_URL` env. | Smoke test passes |
| 6.4 | Contamination controls (entity calibration ledger + perspective tags on all memory writes) | All writes carry `perspective` field |
| 6.5 | **Carmen Beach first real paid booking confirmed** | Stripe shows first non-test charge; payout scheduled |
| 6.6 | Affiliates compensation flow tested with first booking commission | Payout calculated and queued |

**Revenue gate:** **first paying customer landed (Carmen Beach booking).** This is the inflection point.

---

# Day 7 (Wed 2026-05-14) — Fiduciary Risk Audit campaign launch + onboarding

**Goal:** SIE goes outbound with the existing campaign assets. Onboarding bundle landed.

| # | Task | Done when |
|---|---|---|
| 7.1 | Host `fiduciary-risk-audit-landing-page.html` at e.g. `audit.gigaton.ai` (Cloud Run static or Vercel) | Landing page resolves; form submits to a captured-leads inbox |
| 7.2 | Wire form submission → SIE operator-api queue (or simpler: ClickUp + Gmail label) | First test submission flows through |
| 7.3 | Send `fiduciary-risk-audit-email-sequence.html` Day 0 to a starter list of 25–50 fiduciary contacts | Send confirmed in Gmail, replies tracked |
| 7.4 | `gigaton_enterprise_onboarding_v2` → `master-knowledge-base/onboarding/`. Write `START_HERE.md` at org root. | Onboarding bundle deleted from Desktop; `onboarding/` has the canonical entry |
| 7.5 | Per-role guides (operator, dev, decision-reviewer, knowledge-contributor) <500 words each | 4 guides committed |
| 7.6 | Bundle shared with all current users (Notion or Drive folder); link recorded in SERVICES.md | Link live |

**Revenue gate:** outbound campaign is firing. Landing page is collecting leads. Pipeline created.

---

# Day 8 (Thu 2026-05-15) — Public docs + Ti SalesOS bridge

**Goal:** docs.gigaton.ai live. Ti Solutions consuming SalesOS bridge.

| # | Task | Done when |
|---|---|---|
| 8.1 | docs.gigaton.ai on Cloud Run + mkdocs-material container | URL resolves; cert active |
| 8.2 | Publish: SIE concept page, Carmen Beach property guide, Gigaton platform overview, decision certificate explainer (all from existing repo READMEs + Desktop bundles, no new copy) | 4 pages live |
| 8.3 | Unified search across `transcript-knowledge-base` + `master-knowledge-base` exposed to docs site | Search returns hits |
| 8.4 | Ti SalesOS bridge: SalesOS catalog wired into Ti's HubSpot/dialer (read endpoint at minimum) | Ti operators can pull a catalog item from inside their existing tool |
| 8.5 | DNS `dns/` directory in master-knowledge-base records every active domain mapping | Documented |

**Revenue gate:** Ti compounding existing revenue with Gigaton tooling. Public docs reduce sales-call friction.

---

# Day 9 (Fri 2026-05-16) — Policy + first SIE-driven customer action

**Goal:** Human policy codified + machine-enforced. SIE produces a real auto-approved action.

| # | Task | Done when |
|---|---|---|
| 9.1 | `master-knowledge-base/POLICY.md` (authority matrix, deploy windows, AI usage, data handling, comm channels, reversibility) | Committed |
| 9.2 | Policy as YAML in `decision-engine/policy/*.yaml`. 7-gate authorization step actually checks them. | Test confirms denial on policy mismatch |
| 9.3 | SIE auto-approve requires POLICY.md SHA match (halts on policy change until human ack) | SHA mismatch test halts queue |
| 9.4 | First SIE auto-approved action lands in production (T0 trust class only; e.g. canned content edit or low-risk customer reply) | `/sie-audit` shows it; user sees the outcome |
| 9.5 | Verify: ≥3 successful daily deploys logged in `deploy-log.jsonl` over last 5 days | Log shows it |

**Revenue gate:** SIE earns operational trust. Decreases time per action.

---

# Day 10 (Sat 2026-05-17) — Verify, archive, retro, project recurring revenue

**Goal:** Tie the bow. Predictable revenue is **measurable**, not aspirational.

| # | Task | Done when |
|---|---|---|
| 10.1 | Archive abandoned repos (`knowledge-extraction-poc`, `archive-legacy-gigaton-v1`, stale POCs from registry) | GitHub `Archived` flag set |
| 10.2 | Audit: every production service has green `/health`, latest cloudbuild green, custom domain resolves, secrets unexpired | Audit report committed |
| 10.3 | **Revenue snapshot:** count Carmen Beach paid bookings + active affiliate commissions + Gigaton-UI-Platform paying tenants + fiduciary audit pipeline | Numbers in `RETRO_2026-05.md` |
| 10.4 | **30-day projection:** if current run-rate holds, projected MRR/ARR | Documented |
| 10.5 | `RETRO_2026-05.md` — what shipped, what didn't, what's next 30 days. Pin top 3 risks. | Committed |
| 10.6 | Stale Desktop bundles deleted (those folded into repos) | Desktop has fewer than 5 unprocessed items |

**Revenue gate (final):** at least one of these is true:
- ≥3 paid Carmen Beach bookings in 10-day window, OR
- ≥1 paying Gigaton-UI-Platform tenant on a recurring Stripe subscription, OR
- ≥3 booked discovery calls from the fiduciary audit campaign with 1+ converted to commitment.

If zero, the plan failed its revenue test. Retro identifies why and what changes for Cycle 2.

---

# What gets cut if time slips

In priority order (cut from bottom):

1. **Cut first:** Day 8 docs.gigaton.ai — docs can ship Cycle 2; revenue can't.
2. **Cut second:** Day 9 SIE policy/POLICY.md machine encoding — keep human policy doc, defer machine encoding.
3. **Cut third:** Day 6 memory unification — keep Carmen Beach revenue path; defer memory.
4. **Never cut:** Day 1 unblock, Day 2 playadelcarmen.homes live, Day 3 deploy automation, Day 4 billing schema, Day 5 first booking flow.

# Acceptance criteria (Day 10)

- [ ] All 7 critical files exist and are referenced in each prod repo's CLAUDE.md: `SERVICES.md`, `PROCESS.md`, `PRINCIPLES.md`, `POLICY.md`, `START_HERE.md`, `STATE_OF_UNION_2026-05-07.md`, `RETRO_2026-05.md`.
- [ ] `make deploy` works in every production repo. `deploy_all.sh` ships full stack <15 min from clean main.
- [ ] SIE → decision-engine → intelligence-silo end-to-end test passes.
- [ ] `/sie-audit` shows full QC→VC→TC→EC chain on real items.
- [ ] Public docs site live; onboarding bundle shared with users.
- [ ] All stale repos archived; only active repos in the org listing.
- [ ] `deploy-log.jsonl` shows ≥3 successful daily deploys in last 5 days.
- [ ] **Revenue gate met (see Day 10).**
