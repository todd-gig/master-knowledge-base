# V16 Test Plan

## Required checks
- Poynt token scaffolding returns token/order/transaction endpoints
- reconciliation job records drift runs
- webhook replay requires operator note and replays replayable events only
- payment search/filter returns narrowed result sets
- provider health aggregates normalized success/failure counts
- UI tables refresh and render recent payments, events, and provider health
