# Dashboard App

## Purpose
Local operator control plane for:
- memory registry inspection
- namespace overlay review
- retrieval calibration review
- client onboarding visibility

## Run
```bash
npm install
npm run dev
```

## Data source model
This starter app reads local JSON files in `src/data/`.
Replace those files with API-backed fetches when moving to production.
